type: 'library'
language: 'typescript'
platform: 'node'

tasks:
  lint:
    command: 'biome check'
    inputs:
      - 'src/**/*'

  lint-fix:
    command: 'biome check --fix --unsafe'
    inputs:
      - 'src/**/*'

  test:
    command: 'vitest run'
    inputs:
      - 'src/**/*'
      - 'tests/**/*'
